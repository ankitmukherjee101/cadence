import { ScreenPlaceholder } from '@/src/shared/ui/ScreenPlaceholder';

export function JournalScreen() {
  return (
    <ScreenPlaceholder
      title="Journal"
      subtitle="Write reflections tied to a local date. Content stays on-device in v1."
    />
  );
}
